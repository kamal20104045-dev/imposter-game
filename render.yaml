services:
  - type: web
    name: imposter-game-server
    env: node
    branch: main
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars: 
      - key: FRONTEND_ORIGIN
        value: ""
